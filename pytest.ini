[pytest]
# Suppress known deprecation and runtime warnings from test suite
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning

# Ensure async support and test discovery settings
asyncio_mode = auto
testpaths = tests
