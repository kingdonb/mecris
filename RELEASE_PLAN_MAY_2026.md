# Release Plan: May 2026 (The Sovereign UI Update)

## Status: PENDING
**Version Goal:** v1.x.y (To be bumped via `make bump-version`)

---

### 1. Completed Items (Verified in Session)
- [x] **Android UI Refinement**: Fixed the Review Pump 'white line' glitch. Progress bars now use a consistent goal-oriented scale.
- [x] **Security Audit**: Documented unencrypted Oracle coordinates finding in `ARCHITECTURE.md`.
- [x] **Desktop Mode Prep**: Optimized Review Pump gauge dimensions for Pixel 10 Desktop Mode.

### 2. Pre-Release Checklist (Technical Integrity)
- [ ] **Test Suite Validation**: Run full Python and Rust test suites.
  - Command: `make test` or `PYTHONPATH=. .venv/bin/pytest`
- [ ] **Android Build**: Ensure `mecris-go-project` compiles without errors after UI changes.
- [ ] **Version Bump**: Use the official project target to sync all 15+ version strings.
  - Command: `make bump-version VERSION=1.0.x` (Increment accordingly)

### 3. Deployment Strategy (Modality Parity)
- [ ] **Cloud Sync**: Deploy updated logic to Akamai and Fermyon simultaneously.
  - Command: `make deploy-all`
- [ ] **Android APK**: Build and install the new APK on the Pixel 10.
- [ ] **Verification**: Verify that "Cloud Sync" results in a Beeminder datapoint with the correct comment format.

### 4. Next Session Priorities (Immediate Tasks)
1. **Encrypt Oracle Data**: Implement the fix for `location_lat` and `location_lon` in `usage_tracker.py` and Rust `sync-service`.
2. **Sovereign Map Spike**: Prototype the local-only coordinate storage using Android Encrypted Shared Preferences.
3. **Multiplier Sync**: Set the Review Pump lever in-app and verify persistence in Neon (`SELECT pump_multiplier FROM language_stats`).

### 5. Known Issues / Debt
- Oracle location data is plaintext in DB (Documented in ARCHITECTURE.md).
- GPS route points are counted but coordinates are not yet transmitted/stored locally in the new format.

---
*Generated by Gemini CLI - Tuesday, May 19, 2026*
